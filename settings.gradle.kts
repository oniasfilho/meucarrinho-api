plugins {
    // Lets the Java 25 toolchain download itself on machines that don't have it.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "meucarrinho-api"
